import { useEffect, useState } from 'react'

// Custom hooks that compose hooks to abstract common state calculations that can be used in multiple components
export const useCityWeather = city => {
  // calculate weather stuff
};
